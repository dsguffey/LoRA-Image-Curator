# Florence path and FFmpeg monthly acquisition candidate — 2026-09-26

This is candidate qualification evidence, not laptop acceptance or a public release.

## Florence/provider environment

The laptop's Florence failure at step 3/6 was pip `[WinError 206]` while extracting the approved PyTorch wheel into `Apps/LIC-Lite/candidate-1/venv-provider-<id>`. A bounded history search found the path-bound-venv safeguard and Windows long-path checks from M1.4, plus the later Provider Protection transaction, but no prior short final path for provider generations. The previous checks covered the Core installation root, not PyTorch's deeper provider-venv license tree.

New optional-provider generations are created directly at `%USERPROFILE%/LICV/<10-hex-install-root-identity>/p<10-hex-generation>`. The active record and provider journal store the exact final path. The installation-root identity separates different LIC installations; a new generation never overwrites an existing directory. Existing `Apps/LIC-Lite/candidate-1/venv`, `venv-repair-*`, and `venv-provider-*` remain accepted for existing records. Core repair can recognize a new active provider generation. Provider wheels, models, and verified downloads remain in the existing managed Data/cache layout; no resource hashes changed to address the path failure. This venv is per-user and must be treated as managed installation state during future uninstall or Move work.

For `C:\Users\John Smith\Desktop\LoRA Image Curator`, the longest path seen in the locally verified PyTorch wheel's extracted tree projects to under 240 characters at the new provider-venv location, versus over 260 in the former provider-venv location. A real offline installation of the exact cached 1,922,599,627-byte PyTorch `2.13.0+cu130` wheel (SHA-256 `590b2a2b53ef295dcfcd703df17929bd579705bb0e42d9e43c462fe0f0b1e088`) into a disposable long-root venv completed with pip exit code 0 and 15,348 installed files. That first disposable path was under the installation root and required the development host's long-path capability; the final-path budget is separately checked by regression tests. A fresh laptop Florence install is still the acceptance gate.

Dependency installation failures now retain the offline pip command, exit code, bounded output tail, identified package when the error path proves it, and log location in the provider journal. The card identifies the failed dependency stage and cause, including WinError 206, while Technical Details contains the raw diagnostic. On relaunch the same failure is reconstructed from the journal until a new attempt starts.

## FFmpeg replacement

BtbN's official retained monthly tag `autobuild-2026-08-31-13-27` provides `ffmpeg-n8.1.2-50-g1a748fe2cd-win64-lgpl-8.1.zip` (asset 538025211). Its 146,078,616 bytes and SHA-256 `f6274bbd9c247f9e90c1bbed066b03ed4a3907cece2fb91be6dd352393936365` match official GitHub release metadata and the downloaded copy. The archive contains the allowlisted FFmpeg executable (113,981,440 bytes; SHA-256 `9c60da6c0b083110d59084ea39f60ae149aa3e031c3b4bb4f573fafa1c1e7cea`), ffprobe executable (113,775,616 bytes; SHA-256 `67176fa62f89f94c3bcd379fd05677a25651569a2eb8880ec2194e62c82be412`), and LICENSE.txt (7,651 bytes; unchanged SHA-256 `da7eabb7bafdf7d3ae5e9f223aa5bdc1eece45ac569dc21b3b037520b4464768`). The Manager's real archive installation adapter extracted and rehashed exactly those three files in a disposable root. The FFmpeg adapter passed `-version`, LGPL license, build-configuration checks (no GPL/nonfree flags), and local frame-output smoke.

The new immutable component manifest and dated compatibility profile `2026-09-26.1` change only this FFmpeg artifact identity. Prior profiles, including `2026-09-26`, remain intact. Existing installations with the old exact approved FFmpeg profile are still recognized, and resource status uses their installed implementation identity rather than treating those bytes as unapproved merely because a newer profile exists. Nothing automatically deletes or replaces the prior managed FFmpeg files.

## UI and scope

Same-page optional-provider Install/Repair now updates its card in place. Structural refreshes restore the active card's viewport anchor. A provider file download no longer drives the six-stage progress bar with a per-file percentage; the bar remains indeterminate until readiness is verified, and status shows `[stage/6]` with elapsed-time activity. The synchronous full-page rebuild on optional-provider start was removed. This is a bounded improvement; dual overall/per-file progress and a full UI-thread performance investigation remain future work.

Focused provider, recovery, profile, UI, and historical-compatibility tests passed. The official monthly archive was downloaded once; the large Florence model was not downloaded. Existing real LIC resources were not accessed or modified for qualification. The remaining release gate is Product Owner laptop acceptance, especially Florence's final-path wheel installation, optional-provider activation/relaunch, and FFmpeg acquisition from the retained tag. Managed Move currently rebuilds its destination venv under `Apps/LIC-Lite/candidate-1`; moving a full installation to a long root may need a later short-path follow-up before Move is offered for this configuration.
