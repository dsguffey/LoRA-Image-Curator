# Install the appropriate PyTorch/Torchvision build for the target CPU/CUDA
# environment before this file. timm depends on those packages, so reversing
# this order can silently select CPU-only PyTorch on an NVIDIA workstation.
# PyTorch remains outside this static file because its correct official wheel
# depends on the workstation's accelerator and driver stack.

numpy>=1.26,<3
Pillow>=10,<13
# 4.56.2 contains the reviewed native Florence-2 implementation. The app
# intentionally rejects other versions rather than enabling repository code.
transformers==4.56.2
einops>=0.7,<1
timm>=0.9,<2

# Recycle Bin support is a small safety dependency, not an optional analysis
# provider. File deletion never falls back to permanent removal if this package
# is unavailable or the operating system rejects the request.
Send2Trash>=1.8,<3
