# Install the appropriate PyTorch build for the target CPU/CUDA environment
# before this file. PyTorch is deliberately not pinned here because its correct
# wheel depends on the workstation's accelerator and driver stack.

numpy>=1.26,<3
Pillow>=10,<13
transformers==4.49.0
einops>=0.7,<1
timm>=0.9,<2

# Recycle Bin support is a small safety dependency, not an optional analysis
# provider. File deletion never falls back to permanent removal if this package
# is unavailable or the operating system rejects the request.
Send2Trash>=1.8,<3
